; $Id: boron.info,v 1.1.2.5.2.1 2010/10/02 00:04:25 spaceninja Exp $

; Info for the admin themes page
; ------------------------------
name = Boron
description = Boron is a base theme for Drupal 6 which converts the core template files to HTML5 markup.

; Set the Drupal version this theme can be used with
; ------------------------------
core = 6.x

; For sub-themes, uncomment 'base theme' and comment out 'engine'
; ------------------------------
engine = phptemplate
; base theme = boron

; We use [screen] instead of [all] to avoid browser styles affecting
; other media types. IE stylesheets are loaded through template.php
; ------------------------------
stylesheets[screen][] = css/layout.css

; html5.js is required for IE to understand the new elements like article
; ref: http://remysharp.com/2009/01/07/html5-enabling-script/
; ------------------------------
scripts[] = scripts/html5.js

; Information added by drupal.org packaging script on 2010-10-02
version = "6.x-1.0-beta1"
core = "6.x"
project = "boron"
datestamp = "1285978558"

